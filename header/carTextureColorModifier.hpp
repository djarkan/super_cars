#ifndef CARTEXTURECOLORMODIFIER_HPP
#define CARTEXTURECOLORMODIFIER_HPP

#include <SFML/Graphics/Color.hpp>

class CarTextureColorModifier {
    public :
        void        changeColors(sf::Color darkColor, sf::Color mediumColor, sf::Color lightColor);
    private :

};

#endif
